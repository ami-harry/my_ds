// comparsion between array and link list

// array
// random access from array is possible in cosntant time ,,O(1)
// becuase array elements are stored at contigious location



// in link sequential access is possible
// in link list we can't access an element directly. becuase they are store at random position
// accessing of elements depend upon the no of elemens in the link list, O(n)..

// in case of random access, array is better option to use

// array is a fixed size ds

// in link list is memory utilization is better

//memory allocation-->


// insertion
//                          array                    link list
// a. at beginnning         O(n)                        O(1)            
// b. at end                O(1)                        O(n)
// c. in middle             O(n)                        O(n)
// the cost of insertion and deletion is same



// which one is easy to use
// array is easy to use


// searching
// array--> linear and binary search is possible
// in link list--> only liner search is possible

